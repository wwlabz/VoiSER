<Window
    x:Class="VoiSER.Windows.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VoiSER (Windows)">
    <Grid Padding="20" RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="12">
        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="Start / Stop" Click="OnToggleRecordingClick" />
            <TextBlock x:Name="StateLabel" VerticalAlignment="Center" Text="State: Idle" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <TextBlock VerticalAlignment="Center" Text="Output mode:" />
            <ComboBox x:Name="OutputModeCombo" Width="220" SelectionChanged="OnOutputModeChanged">
                <ComboBoxItem Tag="Clipboard" Content="Clipboard" />
                <ComboBoxItem Tag="PasteAtCursor" Content="Paste to active field" />
                <ComboBoxItem Tag="PasteStrict" Content="Strict paste" />
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <CheckBox x:Name="SingleKeyEnabled" Content="Single-key exclusive mode" Checked="OnSingleKeyConfigChanged" Unchecked="OnSingleKeyConfigChanged" />
            <CheckBox x:Name="SingleKeyBlock" Content="Block key in other apps" IsChecked="True" Checked="OnSingleKeyConfigChanged" Unchecked="OnSingleKeyConfigChanged" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8">
            <TextBlock VerticalAlignment="Center" Text="Single key code:" />
            <NumberBox x:Name="SingleKeyCode" Width="120" Minimum="1" Maximum="255" Value="32" ValueChanged="OnSingleKeyCodeChanged" />
            <TextBlock VerticalAlignment="Center" Text="(physical key code)" />
        </StackPanel>

        <TextBox Grid.Row="4" x:Name="LogBox" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>
